# DO NOT set VITE_API_URL to localhost — api.js uses /api which is proxied correctly
# in dev (Vite proxy → localhost:5000) and production (nginx → backend container)
VITE_RAZORPAY_KEY_ID=
VITE_GOOGLE_MAPS_API_KEY=
